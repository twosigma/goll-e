// TODO Remove the eslint flags once this stub is implemented.

/* eslint-disable */
var ParseError = require('./parseError');

/**
 * Finds errors in a graph content AST.
 * @func analyze
 * @param {AST} abstractSyntaxTree an AST of graph content
 * @return {ParseError[]} errors found in the AST.
 */
var analyze = function(abstractSyntaxTree) {
/* eslint-enable */

  // TODO: Check that the abstract syntax tree doesn't violate the rules of how graphs work.

  // For now, we have no implementation for this, so we just do not return any parse errors.
  return [];
};

module.exports = analyze;
